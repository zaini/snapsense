import PatientSubmissionsTable from "../../components/PatientSubmissionsTable";

const PatientSubmissionsPage = () => {
  // TODO you'd pass data to the table from here, so it can show stuff dynamically based on which data you have
  // should work for both patients and doctors.
  // I think this table should be the time that Era is working on with the timelines/table and toggle option.
  return <PatientSubmissionsTable />;
};

export default PatientSubmissionsPage;
