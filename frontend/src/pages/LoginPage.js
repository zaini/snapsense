import React from "react";
import LoginForm from "../components/LoginForm";

const LoginPage = () => {
  return (
    <div>
      <LoginForm name="test" />
    </div>
  );
};

export default LoginPage;
